นี่คือไฟล์ **`README.md`** แบบละเอียดยิบระดับมืออาชีพ (Professional Grade) ที่คุณสามารถนำไปวางใน GitHub หรือโฟลเดอร์โปรเจกต์ของคุณได้เลยครับ ผมเขียนอธิบายครอบคลุมตั้งแต่โครงสร้างไฟล์, วิธีติดตั้ง, ทฤษฎีฟิสิกส์เบื้องหลัง ไปจนถึงคู่มือการใช้งานแต่ละหน้าจอครับ

คุณสามารถคัดลอกโค้ดด้านล่างนี้ไปเซฟเป็นไฟล์ชื่อ `README.md` ได้เลยครับ:

---

```markdown
# 🚀 Projectile & Timing Sync Simulator
**Pneumatic Launcher & Rotating Target 3D Simulation**

โปรแกรมจำลองวิถีโค้งแบบ 3 มิติ (3D Trajectory) และระบบทวนสอบจังหวะเวลา (Timing Sync Validation) สำหรับโปรเจกต์เครื่องยิงลูกสควอชด้วยระบบนิวแมติกส์ โปรแกรมนี้ถูกออกแบบมาเพื่อคำนวณหามุมยิงที่เหมาะสม และหา **"เวลาหน่วง (Firing Delay)"** ที่แม่นยำที่สุด เพื่อให้ลูกบอลพุ่งชนเป้าหมายที่กำลังหมุนอยู่ได้อย่างสมบูรณ์แบบ (Zero Error)

---

## ✨ Features (คุณสมบัติเด่น)
- **Pneumatic Kinematics:** คำนวณความเร็วต้น ($V_0$) อัตโนมัติจากค่าแรงดันลม (Bar), ขนาดกระบอกสูบ (Bore), ระยะชัก (Stroke) และมวลวัตถุ (Mass)
- **Dual Calculation Modes:**
  - `Inverse Mode`: กำหนดความสูงเป้าหมาย แล้วให้โปรแกรมคำนวณหามุมยิง (Pitch, Yaw) ที่ต้องใช้
  - `Forward Mode`: กำหนดมุมยิง แล้วให้โปรแกรมคำนวณหาจุดตกกระทบ
- **Rotating Target Validation:** จำลองเป้าหมายแบบหมุน 3 มิติ พร้อมคำนวณเวลาหน่วง (Delay Time) โดยชดเชยค่าความหน่วงของฮาร์ดแวร์ (Hardware Latency) และเวลาที่ลูกบอลลอยในอากาศ (Flight Time)
- **Highly Optimized 3D Animation:** ระบบแอนิเมชัน 3D ที่ลื่นไหลด้วยเทคนิค Pre-calculation คำนวณล่วงหน้าทุกเฟรม (Step = 0.05s / 20 FPS) ไม่กินทรัพยากรเครื่อง
- **Real-time Kinematics Monitor:** ตรวจสอบตำแหน่ง (Position), ความเร็ว (Velocity) และความเร่ง (Acceleration) ของแกน X, Y, Z ได้แบบ Real-time ตามสถานะ (Delay, HW Latency, Flying, Impact)
- **Save & Load Scenarios:** สามารถบันทึกค่าพารามิเตอร์การทดสอบเป็นไฟล์ `.json` และโหลดกลับมาดูแอนิเมชันใหม่ได้ทันที

---

## 📂 Project Structure (โครงสร้างไฟล์)
โปรเจกต์ประกอบด้วยไฟล์หลัก 4 ไฟล์ และ 1 ไฟล์ข้อมูล:

1. `main_ui.py` - หน้าต่างแผงควบคุมหลัก (Control Panel) สำหรับเลือกโหมดและกรอกพารามิเตอร์ต่างๆ
2. `win3.py` - หน้าต่างแสดงผลลัพธ์และแอนิเมชัน 3 มิติ (มี 2 แท็บ: 3D Trajectory และ 3D Timing Sync)
3. `physic.py` - โมดูลคำนวณสมการฟิสิกส์ (PhysicsEngine) และสมการเวลา (TimingLogic)
4. `tools.py` - โมดูลจัดการไฟล์ (อ่าน/เขียนไฟล์ JSON)
5. `data.json` - ไฟล์ตัวกลางสำหรับส่งผ่านข้อมูลระหว่างหน้าต่าง (สร้างอัตโนมัติเมื่อกด Run)

---

## ⚙️ Installation & Requirements (การติดตั้ง)
โปรแกรมนี้เขียนด้วย Python และใช้ไลบรารีพื้นฐานสำหรับการคำนวณและแสดงผลกราฟ

**Prerequisites:**
- Python 3.8 หรือสูงกว่า
- ไลบรารีที่ต้องติดตั้ง:
  ```bash
  pip install numpy matplotlib

```

*(หมายเหตุ: `tkinter`, `math`, `json` เป็นไลบรารีมาตรฐานที่มาพร้อมกับ Python อยู่แล้ว ไม่ต้องติดตั้งเพิ่ม)*

---

## 📖 How to Use (วิธีใช้งาน)

### Step 1: รันโปรแกรมหลัก

เปิด Command Prompt หรือ Terminal แล้วรันไฟล์:

```bash
python main_ui.py

```

### Step 2: การตั้งค่าพารามิเตอร์ใน Control Panel (`main_ui.py`)

1. **Mode Selection:** - เลือก **Inverse Mode** หากต้องการยิงเป้าหมายที่อยู่บนวงกลมหมุน (โปรแกรมจะล็อกจุด Impact ไว้ที่จุดที่ใกล้ที่สุดเสมอ)
* เลือก **Forward Mode** หากต้องการทดสอบยิงแบบกำหนดมุมเอง


2. **Pneumatic Parameters:** กรอกค่าแรงดันลม (Bar) และขนาดกระบอกสูบ เพื่อให้ระบบคำนวณความเร็วต้น
3. **Target Parameters:**
* `Target Speed (RPM)`: ความเร็วรอบของเป้าหมุน
* `Rotation Dir`: ทิศทางการหมุน (`CCW` ทวนเข็ม หรือ `CW` ตามเข็ม)
* `Trigger Angle (deg)`: มุมติดตั้งเซนเซอร์ (นับทวนเข็มนาฬิกา โดยให้ 0 องศาคือจุดที่เป้าหมุนเข้ามาใกล้ปากกระบอกปืนที่สุด)
* `HW Latency (s)`: ความหน่วงของวาล์วลม/PLC (เวลาตั้งแต่สั่งยิงจนลูกบอลหลุดจากปากกระบอก)


4. **Action:** กด **"🚀 Run Simulation"** เพื่อดูแอนิเมชัน หรือกด **"💾 Save Result"** เพื่อเซฟการตั้งค่านี้ไว้

### Step 3: การวิเคราะห์ผลลัพธ์ (`win3.py`)

เมื่อกดรัน หน้าต่าง 3D จะเด้งขึ้นมาพร้อมองค์ประกอบดังนี้:

* **Left Panel:** แสดงตารางข้อมูล Input, ผลการคำนวณ (Calculated Results), Position Log และ Real-time Monitor (โชว์ Pos, Vel, Acc)
* **View Controls:** ปุ่มปรับมุมมองกล้อง 3D (Top, Front, Side, Default)
* **Animation Controls:** ปุ่ม Start, Stop, Reset แอนิเมชัน
* **Tabs (Right Panel):**
* `🚀 3D Trajectory`: ดูวิถีโค้งพาราโบลาแบบปกติ
* `🎯 3D Timing Sync`: ดูแอนิเมชันเป้าหมุน ระบบจะแสดง **จุด Trigger แบบกะพริบ (สีทอง)** และคำนวณเวลารอ (Delaying) ให้เห็นภาพจริง หากยิงโดนเป้าหมาย Title กราฟจะแสดงคำว่า **"HIT [0.0 deg]"** สีเขียว



---

## 🧠 Core Logic Explained (ทฤษฎีการทำงาน)

### 1. The Closest Point Concept (ลอจิกจุดใกล้สุด)

ในการจำลองเป้าหมุน ระบบถูกตั้งสมมติฐานว่าฐานมอเตอร์ตั้งอยู่ที่ระยะ `X = 2.5m, Y = 0.0m`
ดังนั้นจุดที่เป้าหมายจะหมุนมา **ใกล้ปากกระบอกปืน (0,0,0) มากที่สุด** คือฝั่งซ้ายของวงกลม (พิกัด ) ทางคณิตศาสตร์ถือเป็นมุม **180 องศา** - โปรแกรมจะล็อกจุดตก (Impact Point) ไว้ที่จุดนี้เสมอ เพื่อให้ยิงได้แม่นยำที่สุด

* ผู้ใช้กำหนดมุม Trigger โดยอ้างอิงจากจุดนี้ (0 deg = จุดใกล้สุด)

### 2. Timing Calculation (การคำนวณเวลาหน่วง)

สมการหลักที่ใช้ใน `TimingLogic` (`physic.py`):

1. คำนวณเวลาที่เป้าใช้ในการเดินทางจาก Trigger ไปยังจุด Impact ()
2. ตรวจสอบว่า  มากกว่า **1 วินาที** หรือไม่ (ป้องกันระยะกระชั้นชิดเกินไปจน PLC สั่งงานไม่ทัน หากน้อยกว่า 1 วิ ระบบจะให้รอเป้าหมุนมาใน "รอบถัดไป" อัตโนมัติ)
3. เปรียบเทียบกับเวลาที่ลูกบอลต้องใช้ ()
4. คำนวณหา Delay:



### 3. Kinematics (จลนศาสตร์)

ระบบแบ่งสถานะ (State) ของวัตถุออกเป็น 4 ช่วง เพื่อแสดงในตาราง Real-time Monitor:

* **DELAY:** รอสั่งงาน (Pos = จุดเริ่มต้น, Vel = 0, Acc = 0)
* **HW LATENCY:** วาล์วกำลังเปิด (Pos = จุดเริ่มต้น, Vel = 0, Acc = 0)
* **FLYING:** ลูกบอลลอยในอากาศ มีความเร่งทางแกน Z คือ  ()
* **IMPACT:** ชนเป้าหมาย การกระจัดและความเร็วถูกฟรีซไว้ที่จุดตก

---

## 🤝 Developer & Maintainer

Project developed for **Institute of Field Robotics (FIBO)**
**Engineer:** Passakorn Julapeat

*Powered by Python, Tkinter, and Matplotlib.*